<Window x:Class="IHM.GestionClientsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestion Clients" Height="450" Width="700" 
        WindowStartupLocation="CenterScreen" Background="{StaticResource LightBackground}">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Clients Existants" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,10"/>

                <ListBox x:Name="LstClients" Grid.Row="1" Margin="0,0,0,10" DisplayMemberPath="NomComplet" BorderBrush="#EEE" BorderThickness="1"/>

                <Button x:Name="BtnChoisir" Grid.Row="2" Content="✅ ASSOCIER CE CLIENT" Height="40" 
                        Background="{StaticResource SecondaryColor}" Foreground="White" FontWeight="Bold" Click="BtnChoisir_Click"/>
            </Grid>
        </Border>

        <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Opacity="0.1"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Nouveau Client" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,20"/>

                <TextBlock Text="Nom" Foreground="#555"/>
                <TextBox x:Name="TxtNom" Height="30" Margin="0,0,0,15" VerticalContentAlignment="Center"/>

                <TextBlock Text="Prénom" Foreground="#555"/>
                <TextBox x:Name="TxtPrenom" Height="30" Margin="0,0,0,15" VerticalContentAlignment="Center"/>

                <TextBlock Text="Téléphone" Foreground="#555"/>
                <TextBox x:Name="TxtTel" Height="30" Margin="0,0,0,25" VerticalContentAlignment="Center"/>

                <Button Content="➕ CRÉER CLIENT" Height="40" Click="BtnCreerClient_Click" 
                        Background="{StaticResource PrimaryColor}" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>